/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "img.freepik.com",
      "media.licdn.com",
      "cdn.statically.io",
    ],
    // Add the domain here
  },
};

export default nextConfig;
